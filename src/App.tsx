import React from 'react'

import { Button } from '@material-ui/core';

const App: React.FC = () => {
  return (
    <div className="app">
      <Button variant="contained" color="primary">
        Material
      </Button>
    </div>
  )
}

export default App
